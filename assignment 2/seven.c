#include <stdio.h>
int main()
{
    int no, position = 0;
    printf("Enter the no:");
    scanf("%d", &no);
    // enter your code here.

    printf("Postition of the first 1 in LSB is %d", position);
    return 0;
}